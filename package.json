{
  "name": "sealproof",
  "private": true,
  "type": "module",
  "scripts": {
    "build:browser-spike": "vite build",
    "types": "wrangler types",
    "check": "npm run types && tsc --noEmit && tsc --noEmit --project tests/tsconfig.json && tsc --noEmit --project src/browser-spike/tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.6",
    "typescript": "^6.0.3",
    "vite": "^7.1.5",
    "vitest": "^4.1.0",
    "wrangler": "^4.130.0"
  }
}
